{
  "name": "m2l-server",
  "scripts": {
  },
  "env": {
    "MONGODB_URI_TEST": {
      "required": true
    },
    "EVENTBRITE_BEARER_TOKEN": {
      "required": true
    },
    "SHOPIFY_ACCESS_TOKEN": {
      "required": true
    },
    "REDIS_URL": {
      "required": true
    },
    "SHOPIFY_WEBHOOK_SECRET": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [

  ]
}
