{
  "name": "m2l-server",
  "scripts": {
  },
  "env": {
    "MONGODB_URI_TEST": {
      "required": true
    },
    "EVENTBRITE_BEARER_TOKEN": {
      "required": true
    },
    "SQUARE_PERSONAL_ACCESS_TOKEN": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [

  ]
}
