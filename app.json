{
  "name": "m2l-server",
  "scripts": {
  },
  "env": {
    "MONGODB_URI_TEST": {
      "required": true
    },
    "EVENTBRITE_BEARER_TOKEN": {
      "required": true
    },
    "SQUARE_SANDBOX_APPLICATION_ID": {
      "required": true
    },
    "SQUARE_SANDBOX_ACCESS_TOKEN": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [

  ]
}
